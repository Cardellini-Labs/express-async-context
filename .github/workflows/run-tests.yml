name: Tests

on: [push, pull_request]

jobs:
  test:
    name: Test on Node ${{ matrix.node }} with Express ${{ matrix.express }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
        express: [4, 5]

    steps:
      - name: Clone repo
        uses: actions/checkout@v2

      - name: Set Node.js version
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        run: npm install

      - name: Lint
        if: matrix.node == 24 && matrix.express == 5
        run: npm run lint

      - name: Test with Coverage on Express 5
        if: matrix.express == 5
        run: npm test

      - name: Test with Coverage on Express 4
        if: matrix.express == 4
        run: npm run test:express4

      - name: Publish on Coveralls
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          flag-name: node-${{ matrix.node }}-express-${{ matrix.express }}
          parallel: true

  report-coverage:
    needs: test
    runs-on: ubuntu-latest
    steps:
    - name: Coveralls Finished
      uses: coverallsapp/github-action@master
      with:
        github-token: ${{ secrets.github_token }}
        parallel-finished: true
  
  publish:
    needs: report-coverage
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Clone repo
        uses: actions/checkout@v2

      - name: Set Node.js version
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: Install dependencies
        run: npm install

      - name: Check Version
        run: ./version.sh

      - name: Compile
        run: npm run compile

      - name: Publish to npm
        run: |
          cp .npmrc.config .npmrc
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
